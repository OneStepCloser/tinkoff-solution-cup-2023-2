import React, { FC } from 'react';

import {Page} from '../../components/page/page';

export const MainPage: FC = (props) => {
    return (
        <Page>
            <div>Hello world!</div>
        </Page>
    );
};